#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *a(char *argv[], int n) {
  char *res = &"";
  int i = 0;
  while (argv[n][i] != "\0") {
  }
}

int main(int argc, char *argv[]) {
  int retCode = 0;
  if (argc != 2) {
    printErr("You need to specify the path to exactly one file");
    retCode = 1;
  } else {
	  char a[strlen(argv[1])]=str;
    system("test" + );
    char nomeFile[] = argv[1];
  }
  return retCode;
}
